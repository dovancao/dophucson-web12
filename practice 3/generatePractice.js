'use strict'

function generate(testLengthArray){
   // Remove this line and change to your own algorithm
   let input_map = input.length((target,index,input) => {
		return input[index];
	}
	
}

module.exports = generate
